
# W-2   

- int(1.2) --> removes the decimal part: 1
- int(-1.2) --> removes the decimal part: -1
- round(3.9) --> rounds off: 4
- round(-3.9) --> rounds off: -4
- abs(3) --> mod: 3
- abs(-3) --> mod: 3
- math.floor(2.8): 2
- math.floor(-2.8): -3

f = 3       -- f = 3
d, f = f, 4 -- d = 3,  f = 4

f = 3           
d, f = f, d+1   -- invalid

1) Python evaluates the entire right-hand side before doing any assignments 

- b="hello"  => b[-1:2:-1] #ol   does negative slicing start from last element, then 2nd last element

- [start:stop:step], 
- if step is positive (default) means “go till the end.”
- if step is negative means “go till the beginning.”

- read bout format specifier in python deeply

- print("balloon"'ball')   #valid

- 2.5*2   #5.0
- 2.50*2   #5.00

- a,b = "5"    # error
- a,b = "56"   # here a = "5", b = "6"
- a,b = "567"  # error
- a,b = 5      # error
- a,b = 56     # error
- a,b = 5, 6   # here a = 5, b = 6

### (here 'c' will print)
if False:
    print('ab2')
elif:
    print('c')


- Import statements revise


- len(x) prints number of individual characters in the strings
- ex: len("hello i am rahul") => 16

- revise the usage of .split()
- python donot use ternary operators using (:?)



- break:               used in loop:       stops the current loop
- return():            used in functions:  stops the function immediately
- exit():              used anywhere       stops the entire python program
- sys.exit("bye!") :   used anywhere       stops the entire python program     need to do: import sys


- """ \" """   and   """ " """ both same

- "hello".upper()      : This converts into uppercase
- "hello".upper        : This will return the method object referenced with upper since upper is just referenced, it wont cause any error in the code
- "0.0" is truthy

- "and" has higher precidence than "or"

